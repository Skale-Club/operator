# Incident: Dashboard render error digest `1621801304`

> **Status:** Resolved 2026-05-17 — replaced via SEED-012 phased restoration.
> The minimal dashboard (commit cdcae7f) was the stabilization point. The
> full rich dashboard was rebuilt over six independent waves (D1–D6),
> each shipped as its own commit, with the architectural rule that every
> widget is wrapped in its OWN `WidgetErrorBoundary` + `Suspense` and
> queries its OWN data (no `Promise.all`, no shared fetch). The
> bulletproof inline-styled `error.tsx` from cdcae7f remains in place as
> the segment-level safety net. See
> `.planning/seeds/SEED-012-complete-dashboard.md` for the full plan.


## Symptoms

- Home dashboard (`/`) renders the Next.js production error page.
- Browser stack trace shows `i4 → us → i4 → us` repeating thousands of times — a stack overflow from infinite recursion, NOT a normal async throw.
- Previous fix attempts that wrapped data fetches in `try/catch` did NOT help, confirming the issue is **structural** (render tree), not data-related.

## Stabilization commit

Replaced the home dashboard with a minimal version and made the dashboard error boundary dependency-free.

Files changed:
- `src/app/(dashboard)/error.tsx` — stripped to pure HTML + inline styles. No imports from the dashboard tree. This breaks any feedback loop where the error boundary itself depends on the broken provider/token chain.
- `src/app/(dashboard)/page.tsx` — replaced rich dashboard (metrics, activity feed, pipeline widget, welcome checklist) with a minimal greeting + 5 quick links.

Restoring the full dashboard later:
- `git log -- src/app/(dashboard)/page.tsx` to find the pre-incident version
- restore section-by-section, deploy after each, watch for the error returning

## Investigation status (initial pass)

Files audited for structural recursion hazards — none of these showed an obvious self-render loop:

- `src/app/(dashboard)/layout.tsx` — providers nest: BreadcrumbOverrideProvider → SidebarStateProvider → CommandPaletteProvider → VoiceDeviceShell → CelebrationProvider. All take children; none subscribe to their own state to trigger re-mounts.
- `src/components/onboarding/tour.tsx` — useEffect deps are stable (`[pathname]`, `[active, step]`, `[active]`). No setState in render.
- `src/components/layout/branding-style.tsx` — pure render of a `<style>` tag. No state.
- `src/components/dashboard/welcome-checklist.tsx` — pure presentational, no hooks.
- `src/components/design-system/celebration-provider.tsx` — `useMemo` value, `useCallback` setter. Safe.
- `src/components/calls/twilio-device-provider.tsx` — useEffect dep `[enabled]` is stable from the server-rendered prop. Safe.
- `src/components/layout/page-transition.tsx` — `AnimatePresence` with `key={pathname}`. `pathname` is stable per route. Safe.

## Suspect ranking (to investigate after production is stable)

### High likelihood

1. **`<link rel="icon" href={branding.logoUrl} />` inside dashboard layout body** (line 92-95 of original `layout.tsx`). This is a bare `<link>` outside `<head>`; Next.js Float will hoist it. If `branding.logoUrl` ever returns something that triggers re-fetch on every render (e.g. a signed URL with a per-request timestamp), it could re-hoist and force a layout re-render. **Check whether `getOrgBranding` is memoised across the request and whether `logoUrl` is stable.**

2. **`Suspense` boundaries around server components that throw synchronously in the SAME render frame as their fallback.** The home page has 4 nested `<Suspense>` boundaries (HomeMetrics, ActivityChartSection, ActivityPanel, PipelineWidget). If any of these server components throws BEFORE awaiting (e.g. an import-time module error, or a synchronous `createClient()` throw), the Suspense boundary might trigger the segment error boundary, which then re-renders the page, which throws again.

### Medium likelihood

3. **`OnboardingTour` mounted in layout** (not on the failing page directly, but it's mounted everywhere). `useEffect(setTimeout(setActive, 600))` runs on every pathname change. Safe in isolation, but combined with `PageTransition` (also keyed on pathname) it could create a route-transition feedback loop on very fast re-renders.

4. **`PageTransition` with `AnimatePresence mode="wait"`** — if `pathname` stays the same but the child's identity changes (e.g. a Suspense fallback flickering in/out), AnimatePresence could re-mount children in a loop. Known framer-motion edge case.

### Lower likelihood (already audited, no obvious issue)

5. CelebrationProvider — value memoised.
6. TwilioDeviceProvider — gated behind `enabled` prop from the server; the prop is stable.
7. BrandingStyle — pure.

## Next debugging steps (offline)

After production is stable on the minimal dashboard, restore in this order and deploy after each:

1. **Re-add the hero + WelcomeChecklist** (no Suspense). If still green, that part is innocent.
2. **Re-add the `<VapiSetupBanner />`**.
3. **Re-add `<HomeMetrics />` WITHOUT the Suspense wrapper.** Force the server to await it inline. If this loops, the issue is inside HomeMetrics or its dependencies (`getDashboardMetrics`, `getOrgCostTicker`).
4. **Re-add the Suspense wrapper around HomeMetrics.** If green inline but loops with Suspense, the issue is the Suspense + segment error boundary interaction.
5. **Re-add ActivityChartSection, then ActivityPanel, then PipelineWidget** one at a time.

If a specific component reintroduces the loop, instrument it with `console.log` at module load, function entry, and inside each fetch — match the timing against Vercel logs.

## Why digest `1621801304` specifically points to a recursion loop

In React production builds, function names are mangled to `i4`, `us`, etc. A normal React error stack shows ~5–10 frames before the `renderRootSync` boundary. THOUSANDS of `i4 → us → i4 → us` repeating frames means the same two functions called each other (or were called inside each other's reconciliation cycle) until the JS engine ran out of stack — a hallmark of:

- Error boundary catching → render → throw → catch → render → throw
- A component setting state inside its own render (no useEffect wrapper)
- A `use(promise)` call where the promise resolves into another `use(promise)`

The fact that wrapping data fetches in try/catch did NOT fix it rules out option 3 (any data error would have been caught). That leaves option 1 (the error.tsx + page.tsx loop) and option 2 (a setState in render) as the most likely culprits — both of which the stabilization commit defuses, because `error.tsx` is now provider-free and `page.tsx` is minimal.
