---
phase: 96
plan: "02"
type: tests
autonomous: true
requirements: [SCHED-10, SCHED-11, SCHED-12]
---

# Plan 96-02 — tests/scheduling-bookings.test.ts

## Objective

Integration tests for the server actions `createBooking` and `cancelBookingByToken` using the `vi.mock('@/lib/supabase/server', ...)` pattern from `tests/calls-actions.test.ts`. Mocks: `@/lib/supabase/admin` (service-role), `@/lib/scheduling/google-calendar` (no-op), `@/lib/scheduling/emails` (no-op), `@/lib/rate-limit` (returns allowed).

## Tasks

### Task 1 — Create test file (auto)

Create `tests/scheduling-bookings.test.ts` with 6 test cases:

1. `createBooking` success — valid input + no conflict yields `{ ok: true, data: { id, cancel_token } }`
2. `createBooking` race-condition via SELECT — pre-check returns a conflict, returns `slot_taken`
3. `createBooking` race-condition via 23505 — insert errors with `code: '23505'`, returns `slot_taken`
4. `cancelBookingByToken` success — update returns data, returns `{ ok: true }`
5. `cancelBookingByToken` invalid token — update returns error, returns `not_found_or_already_cancelled`
6. `cancelBookingByToken` already cancelled — update returns null data, returns `not_found_or_already_cancelled`

## Verification

- `npx vitest run tests/scheduling-bookings.test.ts` exits 0
- `npx vitest run tests/scheduling-*.test.ts` exits 0
- `npm run build` exits 0
