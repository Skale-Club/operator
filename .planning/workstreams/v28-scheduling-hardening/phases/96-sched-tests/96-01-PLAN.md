---
phase: 96
plan: "01"
type: tests
autonomous: true
requirements: [SCHED-09]
---

# Plan 96-01 — tests/scheduling-slots.test.ts

## Objective

Unit tests for `generateSlots`. Pure function, no mocks needed (everything is in-memory).

## Tasks

### Task 1 — Create test file (auto)

Create `tests/scheduling-slots.test.ts` with 8 test cases:

1. Returns empty array when availability is `null`
2. Returns slots for a full availability window with no conflicts (9:00–17:00, 30-min slots, UTC, future date) — expect 16 slots
3. Excludes slots overlapping existing bookings
4. Excludes slots overlapping Google Calendar busy times
5. Respects `minAdvanceMinutes` — slots within now+advance window are filtered
6. Respects `durationMinutes` — slots are aligned (e.g. 60-min slots yield half as many)
7. Timezone handling: slots generated in host timezone (e.g. `America/Sao_Paulo`) emit UTC ISO strings whose hour matches the offset
8. End-of-day boundary: `end_time` = `23:30` does not produce a slot ending at or after the next day

## Verification

- `npx vitest run tests/scheduling-slots.test.ts` exits 0
