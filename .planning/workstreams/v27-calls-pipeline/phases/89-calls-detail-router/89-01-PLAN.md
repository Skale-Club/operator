---
plan: 89-01
phase: 89
title: Calls Detail Router — Verify Pre-implemented Code
objective: Verify the existing /calls/[id] detail router and AI/Human variant components match phase goals.
requirements_addressed: [CALL-07, CALL-08]
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/calls/[id]/page.tsx
  - src/components/calls/call-detail-ai.tsx
  - src/components/calls/call-detail-human.tsx
autonomous: true
---

# Plan 89-01: Verify Calls Detail Router

## Objective
Confirm that `/calls/[id]` fetches the unified call, detects type, renders a shared header shell, and delegates to the correct AI or Human variant component.

## Tasks

### Task 1: Verify implementation
Read and confirm all key files exist and implement the phase goals:
- `[id]/page.tsx` — getUnifiedCall, notFound on null, shared PageHeader with avatar+direction, type-based branching
- `call-detail-ai.tsx` — fetches Vapi call row + action logs, buildTimeline for transcript, summary, cost, assistant ID metadata sidebar
- `call-detail-human.tsx` — fetches call_logs for ended_at, CallWaveformPlayer, CallNotesEditor, contact link card

### Task 2: Build gate
`npm run build` exits 0.

## Validation
- [ ] All key files verified
- [ ] npm run build exits 0
