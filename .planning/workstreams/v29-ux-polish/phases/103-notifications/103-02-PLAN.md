---
phase: 103-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 103-01
files_modified:
  - src/components/notifications/notification-bell.tsx
  - src/components/notifications/notification-list.tsx
  - src/components/notifications/notification-item.tsx
  - src/app/(dashboard)/notifications/actions.ts
  - src/components/layout/top-bar.tsx
  - src/app/(dashboard)/layout.tsx
  - tests/notifications/unread-count.test.ts
  - tests/notifications/actions.test.ts
autonomous: true
requirements:
  - NOTIF-02
  - NOTIF-03
  - NOTIF-05

must_haves:
  truths:
    - "Bell icon in header shows a numeric unread count badge (not just a dot)"
    - "Badge shows '9+' when unread > 9, exact number when 1–9, nothing when 0"
    - "Clicking bell opens a Popover inline below the bell (not a sheet or dedicated page) — per D-01"
    - "Popover has a 'Mark all as read' button at the top — per D-05"
    - "Each notification row is clickable and navigates to the relevant resource — per D-06"
    - "Clicking a notification marks it as read individually and closes the popover — per D-06"
    - "Read notifications are visible (dimmer tone) and unread are visually distinct — per D-04"
    - "Supabase Realtime subscription updates the bell badge live on INSERT — per D-07"
    - "userId is passed from dashboard layout → TopBar → NotificationBell (no extra auth fetch)"
  artifacts:
    - path: "src/components/notifications/notification-bell.tsx"
      provides: "Bell button + Popover wrapper + Realtime subscription"
      contains: "use client"
    - path: "src/components/notifications/notification-list.tsx"
      provides: "Scrollable notification list + mark-all button"
      contains: "NotificationList"
    - path: "src/components/notifications/notification-item.tsx"
      provides: "Single notification row with icon, message, timestamp, read styling"
      contains: "NotificationItem"
    - path: "src/app/(dashboard)/notifications/actions.ts"
      provides: "fetchNotifications, markNotificationRead, markAllNotificationsRead server actions"
      contains: "use server"
    - path: "src/components/layout/top-bar.tsx"
      provides: "TopBar wired with userId prop and NotificationBell replacing mock bell"
      contains: "NotificationBell"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "userId passed to TopBar"
      contains: "userId={user.id}"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/top-bar.tsx"
      via: "userId prop added to TopBarProps"
      pattern: "userId.*user\\.id"
    - from: "src/components/notifications/notification-bell.tsx"
      to: "src/app/(dashboard)/notifications/actions.ts"
      via: "fetchNotifications() called in useEffect on mount"
      pattern: "fetchNotifications"
    - from: "src/components/notifications/notification-bell.tsx"
      to: "supabase.channel"
      via: "postgres_changes INSERT subscription filtered by user_id"
      pattern: "postgres_changes.*notifications"
    - from: "src/components/notifications/notification-item.tsx"
      to: "src/app/(dashboard)/notifications/actions.ts"
      via: "markNotificationRead called on click"
      pattern: "markNotificationRead"
---

<objective>
Build the notification bell component, popover panel, and server actions that make the in-app notification system visible and interactive.

Purpose: The DB is ready (plan 01). This plan wires the UI and the data layer so users can see, read, and dismiss notifications in real time.
Output: NotificationBell client component with Realtime subscription, NotificationList + NotificationItem subcomponents, server actions for fetch/mark-read, TopBar and layout updated to pass userId.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/v29-ux-polish/phases/103-notifications/103-CONTEXT.md
@.planning/workstreams/v29-ux-polish/phases/103-notifications/103-RESEARCH.md
@.planning/workstreams/v29-ux-polish/phases/103-notifications/103-01-SUMMARY.md

<interfaces>
<!-- NotificationType and NotificationRow — from plan 01 output (src/types/database.ts) -->
type NotificationType = 'new_conversation' | 'missed_call' | 'flow_failed'

type NotificationRow = Database['public']['Tables']['notifications']['Row']
// Shape:
// {
//   id: string
//   org_id: string
//   user_id: string
//   type: NotificationType
//   payload: Json  -- { conversation_id?, call_log_id?, action_log_id? }
//   read_at: string | null
//   created_at: string
// }

<!-- Supabase browser client (src/lib/supabase/client.ts) -->
import { createClient } from '@/lib/supabase/client'
// Returns createBrowserClient<Database>(url, anonKey)
// Use for Realtime subscriptions (client component only)

<!-- Supabase server client (src/lib/supabase/server.ts) -->
import { createClient, getUser } from '@/lib/supabase/server'
// Use in 'use server' actions — cached per request

<!-- Popover primitive (src/components/ui/popover.tsx) -->
import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover'
// Radix-based, already installed. align="end" sideOffset={8} for top-right placement.

<!-- date-fns relative time (already installed) -->
import { formatDistanceToNowStrict } from 'date-fns'
// formatDistanceToNowStrict(new Date(notification.created_at), { addSuffix: true })

<!-- TopBarProps (src/components/layout/top-bar.tsx) — current shape -->
interface TopBarProps {
  activeOrgId: string | null
  activeOrgName: string | null
  isPlatformAdmin: boolean
}
// Add: userId: string | null

<!-- Navigation target per notification type -->
// new_conversation → /chat?id={payload.conversation_id}
// missed_call      → /calls?highlight={payload.call_log_id}
// flow_failed      → /automations/logs?id={payload.action_log_id}

<!-- CSS design tokens (globals.css) -->
// bg-bg-primary, bg-bg-secondary, bg-bg-tertiary
// text-text-primary, text-text-secondary, text-text-tertiary
// border-border-subtle, border-border
// bg-accent (badge background — project accent color)
// motion-fast (transition utility)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server actions for notifications (fetch, markRead, markAllRead)</name>
  <files>src/app/(dashboard)/notifications/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/chat/actions.ts (server action pattern: 'use server', createClient + getUser from @/lib/supabase/server)
    - src/types/database.ts (NotificationType and notifications table Row shape from plan 01)
    - tests/auth/members-actions.test.ts (how server actions are tested with mocked supabase)
  </read_first>
  <behavior>
    - fetchNotifications(): returns last 30 days of notifications for the current user ordered by created_at DESC, max 50 rows
    - markNotificationRead(id: string): updates read_at = now() on the row matching id AND user_id = user.id AND read_at IS NULL
    - markAllNotificationsRead(): updates read_at = now() on all rows where user_id = user.id AND read_at IS NULL
    - All three functions call getUser() first; return { error: 'Unauthorized' } if no user
    - fetchNotifications uses: created_at > (now - 30 days) filter via `.gte('created_at', thirtyDaysAgo)`
    - RLS via authenticated client handles org scoping automatically — do NOT add .eq('org_id', ...) manually
  </behavior>
  <action>
    Create `src/app/(dashboard)/notifications/actions.ts`:

    ```typescript
    'use server'

    import { createClient, getUser } from '@/lib/supabase/server'
    import type { Database } from '@/types/database'

    export type NotificationRow = Database['public']['Tables']['notifications']['Row']

    export async function fetchNotifications(): Promise<NotificationRow[]> {
      const user = await getUser()
      if (!user) return []
      const supabase = await createClient()
      const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString()
      const { data, error } = await supabase
        .from('notifications')
        .select('*')
        .eq('user_id', user.id)
        .gte('created_at', thirtyDaysAgo)
        .order('created_at', { ascending: false })
        .limit(50)
      if (error) return []
      return data ?? []
    }

    export async function markNotificationRead(id: string): Promise<void> {
      const user = await getUser()
      if (!user) return
      const supabase = await createClient()
      await supabase
        .from('notifications')
        .update({ read_at: new Date().toISOString() })
        .eq('id', id)
        .eq('user_id', user.id)
        .is('read_at', null)
    }

    export async function markAllNotificationsRead(): Promise<void> {
      const user = await getUser()
      if (!user) return
      const supabase = await createClient()
      await supabase
        .from('notifications')
        .update({ read_at: new Date().toISOString() })
        .eq('user_id', user.id)
        .is('read_at', null)
    }
    ```

    Create test scaffold `tests/notifications/actions.test.ts`:
    - Mock `@/lib/supabase/server` to return a mock supabase client and user
    - Test fetchNotifications: mock returns array of rows; assert function returns them
    - Test fetchNotifications with no user: assert returns []
    - Test markNotificationRead: assert .update() is called with { read_at: expect.any(String) } and .eq('id', testId)
    - Test markAllNotificationsRead: assert .update() is called and .is('read_at', null) is chained
    Run `npx vitest run tests/notifications/actions.test.ts` after creation.
  </action>
  <verify>
    <automated>npx vitest run tests/notifications/actions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(dashboard)/notifications/actions.ts` exists with `'use server'` directive
    - `grep "fetchNotifications\|markNotificationRead\|markAllNotificationsRead" src/app/(dashboard)/notifications/actions.ts` returns 3 matches
    - `grep "getUser\|createClient" src/app/(dashboard)/notifications/actions.ts` — both from `@/lib/supabase/server`
    - No `.eq('org_id'` in actions.ts (RLS handles org scoping)
    - `npx vitest run tests/notifications/actions.test.ts` exits 0
  </acceptance_criteria>
  <done>Three server actions exist, tested, no direct org_id filtering, getUser/createClient from server helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationBell, NotificationList, NotificationItem components</name>
  <files>
    src/components/notifications/notification-bell.tsx
    src/components/notifications/notification-list.tsx
    src/components/notifications/notification-item.tsx
    tests/notifications/unread-count.test.ts
  </files>
  <read_first>
    - src/components/ui/popover.tsx (Popover, PopoverContent, PopoverTrigger exports)
    - src/components/layout/top-bar.tsx (existing bell button code to understand what is being replaced)
    - src/app/(dashboard)/notifications/actions.ts (Task 1 output — fetchNotifications, markNotificationRead, markAllNotificationsRead)
    - src/types/database.ts (NotificationRow shape)
  </read_first>
  <behavior>
    notification-bell.tsx:
    - 'use client'
    - Props: { userId: string | null }
    - State: notifications: NotificationRow[], open: boolean
    - Derived: unreadCount = notifications.filter(n => !n.read_at).length
    - Badge label: unreadCount > 9 ? '9+' : unreadCount > 0 ? String(unreadCount) : null
    - On mount: if userId exists, call fetchNotifications() and set state
    - Realtime: subscribe to postgres_changes INSERT on notifications table with filter `user_id=eq.${userId}`; on event prepend new row to state; cleanup on unmount
    - Renders Popover with open/onOpenChange controlled state
    - Bell button: variant="ghost" size="icon-sm", relative positioning for badge
    - Badge: absolute -right-0.5 -top-0.5, min-w-4 h-4, bg-accent, text-white, text-[10px] font-semibold, ring-2 ring-bg-primary
    - PopoverContent: align="end" sideOffset={8} w-80 p-0 — renders NotificationList

    notification-list.tsx:
    - 'use client' (receives callbacks as props)
    - Props: { notifications: NotificationRow[], onMarkAll: () => void, onMarkOne: (id: string) => void, onClose: () => void }
    - Header: "Notifications" title + "Mark all as read" button (disabled if unreadCount === 0)
    - Scrollable body: max-h-96 overflow-y-auto
    - Empty state: "No notifications" centered text when list is empty
    - Maps notifications to NotificationItem components
    - "Mark all as read" calls onMarkAll, which calls markAllNotificationsRead() server action then updates local state (set all read_at to new Date().toISOString())

    notification-item.tsx:
    - 'use client'
    - Props: { notification: NotificationRow, onRead: (id: string) => void, onClose: () => void }
    - On click: call onRead(notification.id) then onClose() then router.push(navigationTarget)
    - Navigation target per type:
      new_conversation → /chat?id={(notification.payload as any).conversation_id}
      missed_call      → /calls?highlight={(notification.payload as any).call_log_id}
      flow_failed      → /automations/logs?id={(notification.payload as any).action_log_id}
    - Visual: unread rows have bg-bg-secondary; read rows have opacity-60
    - Timestamp: formatDistanceToNowStrict(new Date(notification.created_at), { addSuffix: true })
    - Icon per type: MessageCircle for new_conversation, PhoneMissed for missed_call, AlertTriangle for flow_failed (all from lucide-react)
    - Message text per type:
      new_conversation → "New conversation"
      missed_call      → "Missed call"
      flow_failed      → "Automation flow failed"

    unread-count test:
    - Pure logic unit test — no mocking needed
    - Test: given notifications array with 2 unread + 1 read, unreadCount = 2
    - Test: given 10 unread, badge label = '9+'
    - Test: given 0 unread, badge label = null
    - Test: given 9 unread, badge label = '9'
  </behavior>
  <action>
    1. Create `tests/notifications/unread-count.test.ts` FIRST (RED phase):
       Test the badge label and unread count derivation logic as pure functions:
       ```typescript
       // Export a helper from notification-bell or test inline logic
       function getBadgeLabel(unreadCount: number): string | null {
         return unreadCount > 9 ? '9+' : unreadCount > 0 ? String(unreadCount) : null
       }
       ```
       Write 4 test cases for getBadgeLabel. Run the test — it will fail because the function does not exist yet.

    2. Create `src/components/notifications/notification-item.tsx`:
       'use client' component. Import useRouter from 'next/navigation'. Import { MessageCircle, PhoneMissed, AlertTriangle } from 'lucide-react'. Import { formatDistanceToNowStrict } from 'date-fns'. Import NotificationRow type. Implement click handler that calls onRead, onClose, then router.push(). Style: full-width button-like div with hover:bg-bg-secondary, cursor-pointer, px-4 py-3, flex items-start gap-3. Read rows: add opacity-60 class. Timestamp displayed below message text in text-text-tertiary text-xs.

    3. Create `src/components/notifications/notification-list.tsx`:
       'use client' component. Import Button from '@/components/ui/button'. Import NotificationItem. Props as specified. Header row: flex justify-between items-center px-4 py-3 border-b border-border-subtle. "Mark all as read" Button variant="ghost" size="sm" text-xs disabled when no unread. Scrollable area with max-h-96 overflow-y-auto divide-y divide-border-subtle.

    4. Create `src/components/notifications/notification-bell.tsx`:
       'use client'. Implement exactly as described in behavior — useEffect for initial fetch, useEffect for Realtime subscription (separate effect, depends on [userId]), controlled Popover state. Export getBadgeLabel as a named export so the test can import it directly. Wire NotificationList into PopoverContent — pass notifications, onMarkAll (calls markAllNotificationsRead then sets all items read_at), onMarkOne (calls markNotificationRead then updates state), onClose (calls setOpen(false)).

    5. Run `npx vitest run tests/notifications/unread-count.test.ts` — should now pass (GREEN).

    6. Run `npm run build` to confirm no TypeScript errors.
  </action>
  <verify>
    <automated>npx vitest run tests/notifications/unread-count.test.ts 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: notification-bell.tsx, notification-list.tsx, notification-item.tsx
    - `grep "use client" src/components/notifications/notification-bell.tsx` returns match
    - `grep "postgres_changes" src/components/notifications/notification-bell.tsx` returns match
    - `grep "getBadgeLabel" src/components/notifications/notification-bell.tsx` returns match (exported)
    - `grep "Mark all as read" src/components/notifications/notification-list.tsx` returns match
    - `grep "router.push" src/components/notifications/notification-item.tsx` returns match
    - `npx vitest run tests/notifications/unread-count.test.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Three notification components exist, unread-count logic tested, build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire NotificationBell into TopBar and layout</name>
  <files>
    src/components/layout/top-bar.tsx
    src/app/(dashboard)/layout.tsx
  </files>
  <read_first>
    - src/components/layout/top-bar.tsx (current file — understand existing TopBarProps and bell button location)
    - src/app/(dashboard)/layout.tsx (current file — understand how TopBar is rendered and what user data is available)
    - src/components/notifications/notification-bell.tsx (Task 2 output — NotificationBellProps requires userId: string | null)
  </read_first>
  <action>
    Edit `src/components/layout/top-bar.tsx`:
    1. Add `userId: string | null` to `TopBarProps` interface (after `isPlatformAdmin`).
    2. Destructure `userId` from props.
    3. Remove the `const [hasNotifications] = React.useState(false) // mock` line entirely.
    4. Remove the existing `<Tooltip>` block that wraps the bell `<Button>` (the block from `<Tooltip>` to its closing `</Tooltip>`, approximately lines 68–83 in the original file).
    5. In its place, import and render `<NotificationBell userId={userId} />`.
    6. Add import: `import { NotificationBell } from '@/components/notifications/notification-bell'`

    The final right-side icons order must be: OrgSwitcher → DialPadHeaderButton → CommandPalette trigger → NotificationBell → ThemeToggle → PlatformAdmin shield.

    Edit `src/app/(dashboard)/layout.tsx`:
    1. Add `userId={user.id}` prop to the `<TopBar>` JSX element (after `isPlatformAdmin={isPlatformAdmin}`).
    2. No other changes needed — `user` is already available from `await getUser()`.

    After both edits, run `npm run build`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "userId.*string.*null\|userId: string | null" src/components/layout/top-bar.tsx` returns match
    - `grep "NotificationBell" src/components/layout/top-bar.tsx` returns match
    - `grep "hasNotifications" src/components/layout/top-bar.tsx` returns nothing (mock removed)
    - `grep "userId={user.id}" src/app/(dashboard)/layout.tsx` returns match
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>TopBar renders NotificationBell with real userId; mock bell + hasNotifications state removed; build clean.</done>
</task>

</tasks>

<verification>
1. `grep "NotificationBell" src/components/layout/top-bar.tsx` — bell is wired
2. `grep "hasNotifications" src/components/layout/top-bar.tsx` — returns nothing (mock removed)
3. `grep "userId={user.id}" src/app/(dashboard)/layout.tsx` — userId flows from layout
4. `grep "postgres_changes" src/components/notifications/notification-bell.tsx` — Realtime subscription exists
5. `grep "Mark all as read" src/components/notifications/notification-list.tsx` — mark-all button exists
6. `grep "router.push" src/components/notifications/notification-item.tsx` — click navigation exists
7. `npx vitest run tests/notifications/` — all notification tests pass
8. `npm run build` exits 0
</verification>

<success_criteria>
- Bell icon in header shows numeric badge when unread > 0 (NOTIF-02)
- Clicking bell opens popover inline below bell, not a sheet (D-01, NOTIF-03)
- Popover has "Mark all as read" button (D-05, NOTIF-05)
- Each notification is clickable and navigates to the correct resource (NOTIF-03)
- Clicking a notification marks it read individually (D-06, NOTIF-05)
- Read notifications visible in dimmer tone (D-04, NOTIF-05)
- Realtime subscription active — INSERT events update badge live (D-07, NOTIF-02)
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/workstreams/v29-ux-polish/phases/103-notifications/103-02-SUMMARY.md`
</output>
