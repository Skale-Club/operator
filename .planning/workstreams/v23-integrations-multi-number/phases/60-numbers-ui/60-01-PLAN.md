---
phase: 60
plan: 01
title: TwilioPhoneNumbers component + section reorg
status: complete
---

# Plan 60-01: Multi-number UI

## Tasks

1. Create `src/components/integrations/twilio-phone-numbers.tsx` — client component with list + create/edit dialog using shadcn primitives (Dialog, Select, Checkbox, Switch, Textarea, DropdownMenu) and the shared StatusPill/EmptyState
2. Refactor `src/components/integrations/twilio-settings.tsx`:
   - Remove the `From number` input field from the SMS section
   - Remove `fromNumber` state and the `fromNumber: fromNumber.trim() || prev.fromNumber` writeback in `setView`
   - Recompute `smsConfigured`/`voiceConfigured` in `setView` using `prev.numbers.some(...)` capability check
   - Embed `<TwilioPhoneNumbers initial={view.numbers} />` between SMS section and Voice SDK section
   - Add `getDefaultE164(view)` helper that returns the default number's E.164 (falling back to legacy `fromNumber`)
   - Pass `getDefaultE164(view) ?? ''` to TestSmsRow `defaultTo`

## Success criteria

- [x] Component renders list of numbers with friendly_name + E.164 + capability badges + Default pill + kebab menu
- [x] Empty state when zero numbers, with primary CTA to add the first number
- [x] Create/Edit dialog has all 8 fields with client-side validation
- [x] Conditional `forward_to_number` field shows only when routing mode = forward
- [x] Section order in twilio-settings.tsx: Connection status → SMS & basics → Phone numbers → Voice SDK → SIP
- [x] `npm run tsc --noEmit` clean (excluding pre-existing chat-layout)
